import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://github.com/cas74l4n/gabriel.github.io',
    base: '/gabriel.github.io',
});
